export default function SwitchSearch (props) {
    switch(props.content) {
        case 'calendar':
          return 
        case 'keys':
          return
        case 'users':
          return
        case 'active':
        case 'inhabitant':
        case 'rooms':
          return
        default:
          return 'BLAD';
  }
}